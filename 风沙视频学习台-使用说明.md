# 风沙视频学习台 3.2.0 使用说明

## 最快开始

1. 双击桌面的“风沙视频学习台”。
2. 在首页粘贴 YouTube、B 站等视频链接；本地视频或音频请点输入框旁的导入入口。
3. 等待来源字幕获取或本地转录完成，然后进入“学习中心”。
4. 点击逐字稿中的时间戳即可回到对应播放位置；选中文字可让 AI 快速生成笔记、心得或金句卡片。
5. 在“学习笔记”记录洞察、问题、行动、书签、金句和截图，内容会自动保存在本机。
6. 在“学习输出”点击“让 AI 生成”，自动得到学习文稿、重点、复习问题、金句、心得或 Mermaid 图解，再按需编辑、导出 PNG 或写入 Obsidian。

## 推荐设置

- 中文本地转录：优先尝试 `SenseVoice 2025`；首次使用需要下载模型。
- 下载失败：在设置中配置有效的浏览器 Cookies，可提高 YouTube、B 站等平台的成功率。
- AI 学习：先在 AI 设置中配置自己的模型服务。总结与思维导图默认可自动运行，其余工作流按需点击。
- 提示词：在“设置 → 学习 AI 工作流”中调整每种任务的系统提示词；不满意时可以恢复默认版本。
- Mermaid 图解：软件会自动生成、渲染校验并在失败时修复一次。默认先看图，只有需要微调时才展开源码。
- Obsidian：先选择 Vault 和目标目录。遇到同名普通笔记时，软件会要求明确确认，不会直接覆盖。

## 浏览器学习伴侣

1. 在 Chrome 或 Edge 扩展管理页开启开发者模式。
2. 加载仓库构建产物 `apps/extension/.output/chrome-mv3`。
3. 在桌面学习台打开“设置 → 浏览器伴侣”，获取一次性配对码。
4. 打开 YouTube、B 站或其他视频网页，再点击扩展图标完成配对。
5. 可发送当前页面、记录时间点、带上选中文字或截取当前视频画面。

扩展只在用户点击时临时读取当前页面，仅连接本机 `127.0.0.1`，不会持续采集浏览记录。

## 字幕与输出

- 逐字稿支持搜索和时间戳回放。
- 导出支持 TXT、Markdown、JSON、SRT、VTT 等格式。
- 重新转录不会直接抹掉上一版来源和人工校对；可以从历史版本恢复。
- 金句卡片支持版式、主题、比例、字号、署名和 PNG 导出。
- 学习输出中的每种内容块都支持 AI 生成或重新生成；“手动添加”只作为自由补充入口。
- 本地视频可以截取当前画面；纯音频没有视频帧，因此该按钮会禁用。

## 数据与隐私

- 学习笔记：`%APPDATA%\fengsha-video-learning\learning-notebooks.json`
- 设置与课程数据库：`%APPDATA%\fengsha-video-learning\`
- 本地转录在电脑上完成。
- 已启用的自动工作流或按需 AI 生成运行时，相关逐字稿、选中片段和笔记会发送给所配置的模型服务；本地模型服务除外。
- 覆盖安装 3.1.x 不会主动清空上述目录；重要资料仍建议定期备份。

## 当前边界

- 主流程是把链接或本地文件导入桌面学习台，不是在网页视频上持续覆盖实时双语字幕。
- 当前翻译会生成完整的 AI 学习产物，还不会逐句写回播放器形成实时双语字幕覆盖。
- 浏览器伴侣可以主动采集当前页面已有字幕、时间点和画面，但不会常驻监听网页。
- 平台下载能力受网站规则、登录状态、地区限制和 Cookies 有效期影响。
- 自动更新已关闭；新版本请手动运行风沙版安装包覆盖安装。

本软件基于开源项目 VidBee 2.1.0 改造，遵循 MIT 许可证并保留原项目版权声明。
